<view class="container" style="{{containerStyle}}">
  <!-- 修改用户信息头部分 -->
  <view class="user-header" bindtap="onAvatarTap">
    <image class="avatar" src="{{userInfo.avatar}}" mode="aspectFill"/>
    <view class="user-info">
      <text class="nickname">{{userInfo.nickname}}</text>
      <text class="uid">ID: {{userInfo.uid}}</text>
    </view>
  </view>

  <!-- 设置列表 -->
  <view class="settings-list">
    <view class="list-title">账号设置</view>
    <view class="item" wx:for="{{accountSettings}}" wx:key="index" bindtap="handleAccountSettingTap" data-event="{{item.event}}">
      <text>{{item.title}}</text>
      <text wx:if="{{item.value}}" class="value">{{item.value}}</text>
    </view>

    <view class="list-title">通知设置</view>
    <view class="item" wx:for="{{notifySettings}}" wx:key="index">
      <text>{{item.title}}</text>
      <switch class="switch" checked="{{item.enabled}}" bindchange="toggleNotify" data-type="{{item.type}}"/>
    </view>
    
    <view class="list-title">应用信息</view>
    <view class="item">
      <text>版本信息</text>
      <text class="value">v1.0.0</text>
    </view>
    <view class="item" bindtap="handleAboutApp">
      <text>关于瞎记</text>
    </view>
    <view class="item" bindtap="handlePrivacyPolicy">
      <text>隐私政策</text>
    </view>
  </view>
</view>